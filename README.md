# ChatRealtime

A real-time chat application with client and server components.

## Setup

1. Clone the repository
2. Run `docker-compose up` to start the application

## Structure

- `client/`: Frontend React application
- `server/`: Backend Node.js server with Socket.io